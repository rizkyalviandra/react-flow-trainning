// @flow

import * as React from "react";
import ColoredBlock from "./coloredBlock";

const OneWayDataBinding = (): React.Element<*> => {
  return (
    <div>
      <ColoredBlock />
    </div>
  );
};

export default OneWayDataBinding;
